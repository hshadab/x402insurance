# Production-only dependencies (faster deployment)

# Core dependencies
flask>=3.0.0
python-dotenv==1.0.0
httpx==0.25.1
gunicorn==21.2.0
pyyaml==6.0.1

# Rate limiting
flask-limiter==3.5.0
flask-cors==4.0.0

# Monitoring
sentry-sdk[flask]==1.38.0
prometheus-client>=0.19.0

# x402 protocol (pulls in web3>=7, eth-account>=0.12)
x402[flask,evm]==2.0.0

# AgentCore SDK
bedrock-agentcore>=1.2.0

# Background jobs
huey==2.5.0
redis==5.0.1

# Optional: PostgreSQL support
# psycopg2-binary==2.9.9
